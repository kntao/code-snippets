package com.XXX.XXX.query.secondWay;import java.text.DateFormat;import java.text.SimpleDateFormat;import java.util.Date;import java.util.concurrent.ConcurrentLinkedQueue;public class HandleResultDataThread extends Thread{	private ConcurrentLinkedQueue<String> resultsQueue;	private String resultPath;			public HandleResultDataThread(ConcurrentLinkedQueue<String> resultsQueue){		this.resultsQueue = resultsQueue;		resultPath = Factory.getSystemPropertiesValue("resultPath");	}		public void run(){		while(true){			if(!resultsQueue.isEmpty()){				SimpleDateFormat df = new SimpleDateFormat("yyyyMMdd");				String fileName = df.format(new Date()).toString();				String outputFile = resultPath + "result"+fileName + ".txt";				FileOperation.writeOneRowToFile(outputFile, resultsQueue.poll()); // Êä³öÎÄ¼þ			}			else{				try				{					Thread.sleep(1*1000);				}				catch (InterruptedException e)				{					// TODO Auto-generated catch block					e.printStackTrace();				}			}		}	}}